{
  "name": "user_task",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "rollup index.js --file bundle.js --format umd --name 'myBundle'",
    "live-s": "live-server --browser='google chrome'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "app"
  ],
  "author": "eugene",
  "license": "ISC",
  "dependencies": {
    "live-server": "^1.2.2"
  },
  "devDependencies": {
    "rollup": "^4.7.0"
  }
}
